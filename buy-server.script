
host_prefix = "jus-"
for (i = 0; i < 5; i++) {
    purchaseServer(host_prefix + i,1048576)
    scp("hack-target.js","home",host_prefix + i)
    scp("auto-weaken.js","home",host_prefix + i)
    scp("auto-grow.js","home",host_prefix + i)
    scp("auto-hack.js","home",host_prefix + i)
    sleep(6000)
}